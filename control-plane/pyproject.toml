[project]
name = "control-plane"
version = "0.1.0"
description = "lol"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "kubernetes>=36.0.0",
    "python-dotenv>=1.1.0",
]

[project.scripts]
dorothy-chat = "control_plane.cli:main"
